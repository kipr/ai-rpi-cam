#!/bin/sh
set -e

printf "systemd autostart disabled for dev build\nENABLE FOR PROD"
exit 0
UNIT="rpicam-kipr.service"

if [ "${1}" = "remove" ]; then
    systemctl --system disable "${UNIT}" || true
    systemctl --system daemon-reload || true
fi

exit 0
